import React, { ReactElement } from "react";
import "../../assets/css/home/App.scss";

const Card: React.FC = (): ReactElement => {
  return (
    <div className="App">
      Card
    </div>
  );
};

export default Card;
